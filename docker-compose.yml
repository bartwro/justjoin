services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=your_db_url_here

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
